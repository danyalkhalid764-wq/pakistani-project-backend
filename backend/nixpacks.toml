[phases.setup]
nixPkgs = ["python310", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python init_alembic_version.py; alembic upgrade head; uvicorn main:app --host 0.0.0.0 --port $PORT"




